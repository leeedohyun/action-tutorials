name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Print Password
      env:
        MY_PASSWORD: ${{ secret.PASSWORD }}
      run: echo My password is $MY_PASSWORD
